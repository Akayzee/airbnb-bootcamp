export const DEFAULT_LOGIN_REDIRECT = "/";

export const authRoutes = ["/sign-in"];

export const publicRoutes = ["/", "/services", "/experiences", "/listings"];
export const apiAuthPrefix = "/api/auth";
export const listingsPrefix = "/listings";
