import React from "react";

type Props = {
  reservations: object[];
};

const UpcomingReservations = ({ reservations }: Props) => {
  return <div>UpcomingReservations</div>;
};

export default UpcomingReservations;
